{
  "name": "trygonometria",
  "version": "1.0.0",
  "private": true,
  "description": "Interaktywna witryna do nauki trygonometrii — poziom liceum (klasa mat-fiz)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "serve": "node --run serve:py || python3 -m http.server 8788 --directory public"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
